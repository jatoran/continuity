# win

Thin Win32 wrappers: HWND newtypes, COM init, `IVirtualDesktopManager`,
`PerMonitorV2` DPI awareness, monitor enumeration.

Layer: foundation. Depends only on `windows-sys`. Lives below `ui`,
`layout`, `render`, and `input`.
