# Changelog

## 0.2.18

- Coordinate the browser touch-focus arbitration patch; text primitives are
  unchanged.

## 0.2.13

- Coordinate the browser command-rail and IME pointer-placement patch; text
  primitives are unchanged.

## 0.2.12

- Coordinate the browser touch pointer-contract patch; text primitives are
  unchanged.

## 0.2.11

- Coordinate the browser rail-sizing patch; text primitives are unchanged.

## 0.2.10

- Coordinate the mobile command-rail and touch code-copy patch; text
  primitives are unchanged.

## 0.2.9

- Coordinate the line-scoped browser IME composition preview patch; text
  primitives are unchanged.

## 0.2.8

- Coordinate the browser caret-authority and minimal-splice reconcile patch;
  text primitives are unchanged.

## 0.2.7

- Coordinate the browser double-click word and triple-click line-selection
  patch; text primitives are unchanged.

## 0.2.6

- Coordinate the mobile IME composition-hardening SDK patch; text primitives
  are unchanged.

## 0.2.5

- Coordinate the browser selection-foreground and navigation-caret SDK patch;
  text primitives are unchanged.

## 0.2.4

- Coordinate the projection-owned browser selection SDK patch release; text
  primitives are unchanged.

## 0.2.3

- Coordinate the projected visual-caret SDK patch release; text primitives are
  unchanged.

## 0.2.2

- Coordinate the task-caret and projected-pointer SDK patch release; text
  primitives are unchanged.

## 0.2.1

- Keep unbroken list-item content on the marker's first display row.

## 0.2.0

- Coordinated embedded SDK release with viewport-scoped browser presentation.

## 0.1.3

- Coordinated SDK patch release; text primitives are unchanged.

## 0.1.2

- Coordinated SDK patch release; text primitives are unchanged.

## 0.1.1

- Coordinated SDK patch release; text primitives are unchanged.

## 0.1.0

- Initial preview of Continuity text coordinates, selections, ranges, and edit primitives.
