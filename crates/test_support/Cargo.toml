[package]
name             = "continuity-test-support"
version          = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true

[features]
default = ["native-harness"]
native-harness = [
    "dep:continuity-text",
    "dep:continuity-buffer",
    "dep:continuity-persist",
    "dep:continuity-core",
    "dep:continuity-host",
    "dep:continuity-ui",
    "dep:continuity-command",
    "dep:continuity-keymap",
    "dep:continuity-win",
    "dep:crossbeam-channel",
    "dep:tempfile",
    "dep:proptest",
    "dep:windows",
]

[dependencies]
continuity-text    = { workspace = true, optional = true }
continuity-buffer  = { workspace = true, optional = true }
continuity-persist = { workspace = true, optional = true }
continuity-core    = { workspace = true, optional = true }
continuity-host    = { workspace = true, optional = true }
# Phase 17.9 §C: win32_harness needs these for spawning a hidden
# Window on its own message-pump thread. They were dev-deps before
# 17.9; moved to regular deps so `src/win32_harness.rs` can use
# them. test_support is itself dev-only, so this doesn't bloat
# production builds.
continuity-ui      = { workspace = true, optional = true }
continuity-command = { workspace = true, optional = true }
continuity-keymap  = { workspace = true, optional = true }
continuity-win     = { workspace = true, optional = true }
continuity-test-fixtures = { workspace = true }
crossbeam-channel  = { workspace = true, optional = true }
tempfile           = { workspace = true, optional = true }
proptest           = { workspace = true, optional = true }
windows            = { workspace = true, optional = true, features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_Graphics_Gdi",
] }

[dev-dependencies]
continuity-decorate = { workspace = true }
continuity-search   = { workspace = true }
serde_json          = { workspace = true }
dhat                = { workspace = true }
