# Changelog

## 0.2.18

- Coordinate the browser touch-focus arbitration patch; buffer and undo
  contracts are unchanged.

## 0.2.13

- Coordinate the browser command-rail and IME pointer-placement patch; buffer
  and undo contracts are unchanged.

## 0.2.12

- Coordinate the browser touch pointer-contract patch; buffer and undo
  contracts are unchanged.

## 0.2.11

- Coordinate the browser rail-sizing patch; buffer and undo contracts are
  unchanged.

## 0.2.10

- Coordinate the mobile command-rail and touch code-copy patch; buffer and
  undo contracts are unchanged.

## 0.2.9

- Coordinate the line-scoped browser IME composition preview patch; buffer and
  undo contracts are unchanged.

## 0.2.8

- Coordinate the browser caret-authority and minimal-splice reconcile patch;
  buffer and undo contracts are unchanged.

## 0.2.7

- Coordinate the browser double-click word and triple-click line-selection
  patch; buffer and undo contracts are unchanged.

## 0.2.6

- Coordinate the mobile IME composition-hardening SDK patch; buffer and undo
  contracts are unchanged.

## 0.2.5

- Coordinate the browser selection-foreground and navigation-caret SDK patch;
  buffer and undo contracts are unchanged.

## 0.2.4

- Coordinate the projection-owned browser selection SDK patch release; buffer
  and undo contracts are unchanged.

## 0.2.3

- Coordinate the projected visual-caret SDK patch release; buffer and undo
  contracts are unchanged.

## 0.2.2

- Coordinate the task-caret and projected-pointer SDK patch release; buffer
  and undo contracts are unchanged.

## 0.2.1

- Coordinate the patch release containing shared list-wrap behavior.

## 0.2.0

- Coordinated embedded SDK release with browser navigation and plain-text surfaces.

## 0.1.3

- Coordinated SDK patch release; buffer and undo contracts are unchanged.

## 0.1.2

- Coordinated SDK patch release; buffer and undo contracts are unchanged.

## 0.1.1

- Coordinated SDK patch release; buffer and undo contracts are unchanged.

## 0.1.0

- Initial preview of the rope-backed Continuity buffer and undo data model.
